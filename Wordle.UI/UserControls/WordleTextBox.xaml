<UserControl x:Class="Wordle.UI.UserControls.WordleTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Wordle.UI.UserControls"
             xmlns:model="clr-namespace:Wordle.UI.Model"
             xmlns:vm="clr-namespace:Wordle.UI.ViewModel"
             xmlns:services="clr-namespace:Wordle.UI.Services"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="ListView">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <ItemsPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" services:PanelGapSetter.ColumnGap="10" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate DataType="{x:Type vm:LetterViewModel}">
                        <TextBox Width="35"
                            FontSize="20"
                            HorizontalContentAlignment="Center"
                            MaxLength="1"
                            CharacterCasing="Upper"
                            IsEnabled="False"
                            Padding="{Binding RelativeSource={RelativeSource AncestorType=controls:WordleTextBox}, Path=Padding}"
                            Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            TextChanged="OnTextChangedMove"
                            PreviewKeyDown="OnBackspaceKeyDown">

                            <TextBox.Resources>
                                <CornerRadius
                                    x:Key="RoundedTextBox.CornerRadius"
                                    TopLeft="10"
                                    TopRight="10"
                                    BottomLeft="10"
                                    BottomRight="10" />
                            </TextBox.Resources>

                            <TextBox.Style>
                                <Style TargetType="TextBox" BasedOn="{StaticResource textBoxStyle}">
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="1" />
                                        </Trigger>
                                        <DataTrigger
                                            Binding="{Binding Path=State}"
                                            Value="{x:Static model:LetterState.Guessed}">
                                            
                                            <Setter Property="Background" Value="Green" />
                                        </DataTrigger>
                                        <DataTrigger
                                            Binding="{Binding Path=State}"
                                            Value="{x:Static model:LetterState.Present}">
                                            
                                            <Setter Property="Background" Value="Yellow" />
                                        </DataTrigger>
                                        <DataTrigger
                                            Binding="{Binding Path=State}"
                                            Value="{x:Static model:LetterState.Wrong}">
                                            
                                            <Setter Property="Background" Value="Red" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </UserControl.Resources>

    <Grid>
        <ListView
            ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=controls:WordleTextBox}, Path=Letters}"
            Loaded="SetupTextInputList" />
    </Grid>
</UserControl>
